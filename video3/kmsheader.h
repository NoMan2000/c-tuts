int timeRanInMinutes, timeRanInSeconds, milesToTotalTimeInSeconds, secondsPerMile, totalTimeInSeconds, minutesPerMile;
double kms, miles, milesRan;     

void calculateHours(double milesRan);
/* Function prototype.  C normally executes from top-to-bottom.  To enable the ability to call a function before it is defined, a function prototype is created. */